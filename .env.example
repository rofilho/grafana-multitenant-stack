# ======================================================
# MONITORING STACK — Environment Variables
# ======================================================
# 1. Copy this file: cp .env.example .env
# 2. Fill in all values marked with <CHANGE_ME>
# 3. NEVER commit the .env file to git
# ======================================================

# --- Grafana ---
GRAFANA_ADMIN_PASSWORD=<CHANGE_ME>        # Strong password for admin access
GRAFANA_DOMAIN=localhost                   # Your domain, e.g: monitoring.yourdomain.com
GRAFANA_VERSION=10.4.2

# --- Ingest Credentials (used by client agents to push metrics/logs) ---
INGEST_USER=ingest
INGEST_PASSWORD=<CHANGE_ME>               # Strong password — clients will use this

# --- Service Versions ---
LOKI_VERSION=3.0.0
MIMIR_VERSION=2.12.0
TEMPO_VERSION=2.4.1
UPTIME_KUMA_VERSION=1

# --- (Optional) GCP / BigQuery Integration ---
# Only needed if you use the docker-compose.gcp.yml add-on
# GCP_PROJECT_ID=your-gcp-project-id
# BIGQUERY_CLIENT_EMAIL=your-sa@your-project.iam.gserviceaccount.com
# BIGQUERY_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
