# ============================================================
# Central stack settings
# ============================================================

# Public hostname or IP of the central monitoring server
# Used by client agents to reach ingest endpoints
STACK_HOST=monitoring.example.com

# External HTTP port exposed by Nginx
STACK_PORT=80

# ============================================================
# Grafana
# ============================================================
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=changeme-admin-password
GF_SECRET_KEY=changeme-secret-key-32chars-minimum

# Optional SMTP for alerting
GF_SMTP_ENABLED=false
GF_SMTP_HOST=smtp.example.com:587
GF_SMTP_USER=
GF_SMTP_PASSWORD=
GF_SMTP_FROM_ADDRESS=grafana@example.com
